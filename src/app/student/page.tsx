import React from 'react';
import StudentsList from '../../components/StudentsList'; // Adjust the path as needed

const StudentListPage = () => {
  return (
    <div>
      <StudentsList />
    </div>
  );
};

export default StudentListPage;
